# Agent Scripts

Runtime behaviour of AI-controlled characters.

- **Path-following** — moves agent along a computed node path at configurable speed
- **Rotation** — smoothly steers agent to face the next waypoint
- **Animation blending** — drives Animator parameters (speed, turning) from movement state
